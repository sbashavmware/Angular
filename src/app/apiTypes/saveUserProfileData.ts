/*
 * ISaveUserProfile used to capture the status of the save operation
 */
export  interface  ISaveUserProfile{
    /** status property */
    status: string;
    /**message property */
    message: string;
}